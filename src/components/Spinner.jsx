import spinnerImage from '../assets/spinner.png';

function Spinner() {
  return <img src={spinnerImage} alt="Loading..." className="spinner" />; // Displaying loading spinner ⏳
}

export default Spinner;
